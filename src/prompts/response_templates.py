"""
回答生成 Prompt 模板（三种路由）。

按 route 类型分别定义：RAG（基于检索片段）、Tool（工具结果汇总）、LLM（通用对话）三种模板。
"""

# ── RAG 路由：基于检索内容生成回答 ─────────────────────────────
RAG_RESPONSE_PROMPT = """你是一个学术论文问答助手。请根据以下检索到的论文片段，回答用户的问题。

当前会话情景（最近操作记录）：
{context}

检索到的相关内容：
{contexts}

对话历史：
{history}

用户问题：{user_input}

要求：
1. 优先基于检索内容回答；即使片段来自实验章节或结论，也应尝试综合归纳出有意义的回答
2. 只有当检索内容与问题完全无关（如问天气却只检索到数学公式）时，才说明"根据现有知识库无法回答此问题"
3. 引用具体来源时请注明（如"根据论文 XXX 第 N 页"）
4. 如果用户要求简短介绍，请控制在 2-3 句话以内"""

# ── Tool 路由：汇总工具执行结果 ──────────────────────────────────
TOOL_RESPONSE_PROMPT = """你是一个学术助手。以下是工具执行的结果，请将其整理为清晰易读的回答。

工具执行结果：
{tool_result}

用户原始请求：{user_input}

要求：
1. 用自然语言汇总工具结果
2. 保留关键信息（论文标题、ID、状态等）
3. 如果工具执行失败，如实说明"""

# ── LLM 路由：通用对话 ──────────────────────────────────────────
LLM_RESPONSE_PROMPT = """你是 GradCopilot，一个学术研究助手。

对话历史：
{history}

用户输入：{user_input}

注意：本次回答不使用知识库，为通用对话模式。"""
